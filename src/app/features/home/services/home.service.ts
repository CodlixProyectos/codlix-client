import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class HomeService {

  constructor() { }

  // Aquí puedes agregar métodos para obtener datos
  // Por ejemplo: getServices(), getTestimonials(), etc.
}
